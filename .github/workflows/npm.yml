name: 'NPM Publish'

on:
  push:
    tags:
      - v*

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm i
      - run: npm run lint
      - run: npm run build
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_KEY }}
